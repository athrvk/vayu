registry=https://registry.npmjs.org/
# Only npm's own settings belong in this file. pnpm reads its settings - the
# release-age policy, the build allowlist - from pnpm-workspace.yaml, and
# pnpm 11 ignores them here, so a pnpm key added to .npmrc looks active and
# does nothing.

# Formatting for the C++ engine (engine/src, engine/include, engine/tests).
#
# Derived from the code, not imported: every setting below was chosen by
# measuring the bulk-format diff it produces over the 285 non-vendor engine
# translation units and keeping whichever value the engine's existing code
# already agrees with. Issue #886 has the full table. Two conclusions are worth
# keeping next to the config, because they read as mistakes otherwise:
#
#   - `ColumnLimit: 80` with `PenaltyExcessCharacter: 1`. The penalty is what
#     makes 80 a target rather than a wall: 90% of engine lines are already
#     under 80, and the ~6% that are over are almost all under 100. Raising the
#     limit instead (90/100/120) makes the diff two to three times *worse*,
#     because a wider limit re-joins line breaks the code chose by hand.
#   - `ContinuationIndentWidth: 0`. Unusual, and the code is written to it -
#     wrapped arguments sit at the enclosing block indent. **Decided and kept**
#     (#940, closing #907), by re-running #886's measurement at clang-format 19
#     over the 288 non-vendor sources on master at be9be9a:
#
#         config                                files rewritten   lines
#         as shipped                                          0       0
#         ContinuationIndentWidth: 4                        250   23938
#         ContinuationIndentWidth: 4 + AlignAfterOpenBracket: Align
#                                                           250   28105
#
#     That is 3.3x and 3.8x #886's whole bulk-format commit (149 files, 7362
#     lines), and it is not old code disagreeing: of the 191 engine sources
#     touched in the last 60 engine commits, `ContinuationIndentWidth: 4` would
#     rewrite 180. People writing engine code today write this style. The
#     question is closed - do not reopen it without a measurement that says
#     something new.
#
# clang-format 19 is the pinned version (CONTRIBUTING.md); the bulk-format
# commit in .git-blame-ignore-revs was produced by it.
#
# engine/vendor/ carries `DisableFormat: true` and is never touched.

Language: Cpp
BasedOnStyle: LLVM

# Indentation: 4 spaces, no tabs, no extra indent for namespaces or access
# modifiers, and wrapped lines at the enclosing block indent.
IndentWidth: 4
TabWidth: 4
AccessModifierOffset: 0
ContinuationIndentWidth: 0
ConstructorInitializerIndentWidth: 0

# Wrapping: never realign to an open bracket or an operator - the engine wraps
# to the block indent instead. Declaration parameters go all-on-one-line or
# one-per-line; call arguments stay bin-packed.
AlignAfterOpenBracket: DontAlign
AlignOperands: DontAlign
AlignEscapedNewlines: Left
AllowAllParametersOfDeclarationOnNextLine: false
BinPackParameters: false
BreakBeforeTernaryOperators: false
Cpp11BracedListStyle: false

# Column budget. See the note above: the limit is soft by design.
ColumnLimit: 80
PenaltyExcessCharacter: 1
PenaltyBreakBeforeFirstCallParameter: 100
PenaltyBreakComment: 100
PenaltyBreakFirstLessLess: 0
PenaltyBreakString: 100
PenaltyReturnTypeOnItsOwnLine: 20

# Single-line forms: only `case X: return y;` collapses.
AllowShortCaseLabelsOnASingleLine: true
AllowShortFunctionsOnASingleLine: None

# Comments are left alone: the engine's block comments are hand-wrapped prose
# that reflowing scrambles.
#
# Includes ARE sorted. The tree was already 99.8% sorted before this config
# landed - turning the sorter on cost 7 files and 18 lines - so enforcing it is
# nearly free and buys a guarantee no reviewer has to supply by hand.
#
# Exactly one include order in the engine is load-bearing, and it is pinned at
# the site rather than by disabling the sorter everywhere:
# `engine/src/platform/platform_windows.cpp` wraps `<windows.h>` /
# `<timeapi.h>` in `// clang-format off` because timeapi.h uses types
# (MMRESULT, UINT, WINAPI) that windows.h defines, does not compile standalone,
# and sorts first alphabetically. Nothing catches that but the Windows CI leg.
# If you find a second such case, pin it the same way - do not turn the sorter
# off again for one file.
ReflowComments: false
SortIncludes: CaseSensitive
CommentPragmas: ''

# Spacing.
PointerAlignment: Left
SpaceBeforeParens: Always
SpacesInContainerLiterals: false
AlignConsecutiveAssignments: Consecutive
MaxEmptyLinesToKeep: 2

[
  {
    "category": "inherit",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "elementId": {
          "description": "The id of an inherited element to drop from this request or collection's resolved list.",
          "minLength": 1,
          "title": "Element to disable",
          "type": "string"
        }
      },
      "required": [
        "elementId"
      ],
      "type": "object"
    },
    "description": "Drops one element inherited from an ancestor collection, named by id, out of this request or collection's resolved list.",
    "hotPathClass": "declarative",
    "kind": "inherit.disable",
    "label": "Disable inherited element",
    "phases": [],
    "version": 1
  },
  {
    "category": "script",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "script": {
          "description": "The JavaScript source to execute.",
          "title": "Script",
          "type": "string"
        }
      },
      "required": [
        "script"
      ],
      "type": "object"
    },
    "description": "Runs before the request is sent, with the pm API. Edits to pm.request change what is actually sent. Never runs under a load test - only on Send and in a collection run.",
    "hotPathClass": "script",
    "kind": "script.pre",
    "label": "Pre-request script",
    "phases": [
      "step.before"
    ],
    "version": 1
  },
  {
    "category": "script",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "script": {
          "description": "The JavaScript source to execute.",
          "title": "Script",
          "type": "string"
        }
      },
      "required": [
        "script"
      ],
      "type": "object"
    },
    "description": "Runs after the response is received. Use pm.test() for assertions: pm.response.to asserts about the response itself, pm.expect asserts about any value handed to it.",
    "hotPathClass": "script",
    "kind": "script.post",
    "label": "Post-request script",
    "phases": [
      "step.after"
    ],
    "version": 1
  },
  {
    "category": "script",
    "collectionOnly": true,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "script": {
          "description": "The JavaScript source to execute.",
          "title": "Script",
          "type": "string"
        }
      },
      "required": [
        "script"
      ],
      "type": "object"
    },
    "description": "Runs once, on a collection, before the run starts.",
    "hotPathClass": "script",
    "kind": "script.setup",
    "label": "Setup script",
    "phases": [
      "run.start"
    ],
    "version": 1
  },
  {
    "category": "script",
    "collectionOnly": true,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "script": {
          "description": "The JavaScript source to execute.",
          "title": "Script",
          "type": "string"
        }
      },
      "required": [
        "script"
      ],
      "type": "object"
    },
    "description": "Runs once, on a collection, after the run ends.",
    "hotPathClass": "script",
    "kind": "script.teardown",
    "label": "Teardown script",
    "phases": [
      "run.end"
    ],
    "version": 1
  },
  {
    "category": "extract",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "default": {
          "description": "Written to the variable when there is no match.",
          "title": "Fallback value",
          "type": "string"
        },
        "matchNo": {
          "description": "Selects among multiple matches: 1-based index, 0 for random, -1 to write every match.",
          "title": "Which match",
          "type": "integer",
          "x-vayu-group": "advanced"
        },
        "path": {
          "description": "A JSONPath subset ($.a.b, [n], [*], ..name) locating the value to extract.",
          "pattern": "^\\$(\\.[A-Za-z0-9_]+|\\.\\.[A-Za-z0-9_]+|\\[\\*\\]|\\[-?[0-9]+\\])*$",
          "title": "JSONPath",
          "type": "string"
        },
        "required": {
          "description": "Fails the step's assertions when no match is found.",
          "title": "Required",
          "type": "boolean"
        },
        "scope": {
          "description": "Which variable scope to write the result into.",
          "enum": [
            "env",
            "collection",
            "globals"
          ],
          "title": "Store in",
          "type": "string"
        },
        "variable": {
          "description": "The variable name to write the result into.",
          "minLength": 1,
          "title": "Variable name",
          "type": "string"
        }
      },
      "required": [
        "path",
        "variable"
      ],
      "type": "object"
    },
    "description": "Reads one value out of the JSON response body by a JSONPath subset ($.a.b, [n], [*], ..name) into a variable.",
    "hotPathClass": "declarative",
    "kind": "extract.json",
    "label": "Extract from JSON",
    "phases": [
      "step.after"
    ],
    "version": 1
  },
  {
    "category": "extract",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "default": {
          "description": "Written to the variable when there is no match.",
          "title": "Fallback value",
          "type": "string"
        },
        "field": {
          "description": "Which part of the response to search: body, headers, url or status.",
          "enum": [
            "body",
            "headers",
            "url",
            "status"
          ],
          "title": "Field",
          "type": "string"
        },
        "matchNo": {
          "description": "Selects among multiple matches: 1-based index, 0 for random, -1 to write every match.",
          "title": "Which match",
          "type": "integer",
          "x-vayu-group": "advanced"
        },
        "pattern": {
          "description": "The regular expression to run against the response.",
          "minLength": 1,
          "title": "Pattern",
          "type": "string"
        },
        "required": {
          "description": "Fails the step's assertions when no match is found.",
          "title": "Required",
          "type": "boolean"
        },
        "scope": {
          "description": "Which variable scope to write the result into.",
          "enum": [
            "env",
            "collection",
            "globals"
          ],
          "title": "Store in",
          "type": "string"
        },
        "template": {
          "description": "A $1$-style template of the match's capture groups; $0$ is the whole match.",
          "title": "Replacement template",
          "type": "string"
        },
        "variable": {
          "description": "The variable name to write the result into.",
          "minLength": 1,
          "title": "Variable name",
          "type": "string"
        }
      },
      "required": [
        "pattern",
        "variable"
      ],
      "type": "object"
    },
    "description": "Runs a regular expression against the response and writes a match (or a $1$-style template of its groups) into a variable.",
    "hotPathClass": "declarative",
    "kind": "extract.regex",
    "label": "Extract with a regular expression",
    "phases": [
      "step.after"
    ],
    "version": 1
  },
  {
    "category": "extract",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "default": {
          "description": "Written to the variable when there is no match.",
          "title": "Fallback value",
          "type": "string"
        },
        "header": {
          "description": "The response header to read, matched case-insensitively.",
          "minLength": 1,
          "title": "Header name",
          "type": "string"
        },
        "required": {
          "description": "Fails the step's assertions when no match is found.",
          "title": "Required",
          "type": "boolean"
        },
        "scope": {
          "description": "Which variable scope to write the result into.",
          "enum": [
            "env",
            "collection",
            "globals"
          ],
          "title": "Store in",
          "type": "string"
        },
        "variable": {
          "description": "The variable name to write the result into.",
          "minLength": 1,
          "title": "Variable name",
          "type": "string"
        }
      },
      "required": [
        "header",
        "variable"
      ],
      "type": "object"
    },
    "description": "Copies one response header's value into a variable.",
    "hotPathClass": "declarative",
    "kind": "extract.header",
    "label": "Extract a response header",
    "phases": [
      "step.after"
    ],
    "version": 1
  },
  {
    "category": "extract",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "default": {
          "description": "Written to the variable when there is no match.",
          "title": "Fallback value",
          "type": "string"
        },
        "leftBoundary": {
          "description": "The text immediately before the value to extract.",
          "title": "Left boundary",
          "type": "string"
        },
        "matchNo": {
          "description": "Selects among multiple matches: 1-based index, 0 for random, -1 to write every match.",
          "title": "Which match",
          "type": "integer",
          "x-vayu-group": "advanced"
        },
        "required": {
          "description": "Fails the step's assertions when no match is found.",
          "title": "Required",
          "type": "boolean"
        },
        "rightBoundary": {
          "description": "The text immediately after the value to extract.",
          "title": "Right boundary",
          "type": "string"
        },
        "scope": {
          "description": "Which variable scope to write the result into.",
          "enum": [
            "env",
            "collection",
            "globals"
          ],
          "title": "Store in",
          "type": "string"
        },
        "variable": {
          "description": "The variable name to write the result into.",
          "minLength": 1,
          "title": "Variable name",
          "type": "string"
        }
      },
      "required": [
        "variable"
      ],
      "type": "object"
    },
    "description": "Reads the text between a left and a right boundary out of the response body into a variable (JMeter's Boundary Extractor).",
    "hotPathClass": "declarative",
    "kind": "extract.boundary",
    "label": "Extract between two boundaries",
    "phases": [
      "step.after"
    ],
    "version": 1
  },
  {
    "category": "assert",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "in": {
          "description": "A set of status codes the response must match one of.",
          "items": {
            "type": "integer"
          },
          "title": "Accepted codes",
          "type": "array"
        },
        "range": {
          "additionalProperties": false,
          "description": "The response status must fall within this inclusive range.",
          "properties": {
            "max": {
              "description": "The highest acceptable status code, inclusive.",
              "title": "Maximum",
              "type": "integer"
            },
            "min": {
              "description": "The lowest acceptable status code, inclusive.",
              "title": "Minimum",
              "type": "integer"
            }
          },
          "required": [
            "min",
            "max"
          ],
          "title": "Accepted range",
          "type": "object"
        }
      },
      "type": "object"
    },
    "description": "Fails the step unless the response status is in a set or a range.",
    "hotPathClass": "declarative",
    "kind": "assert.status",
    "label": "Assert status code",
    "phases": [
      "step.after"
    ],
    "version": 1
  },
  {
    "category": "assert",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "exists": {
          "description": "Passes when at least one match is found.",
          "title": "Must exist",
          "type": "boolean"
        },
        "expected": {
          "description": "The exact value the match must equal.",
          "title": "Expected value"
        },
        "negate": {
          "description": "Flips the assertion's pass/fail verdict.",
          "title": "Negate",
          "type": "boolean"
        },
        "path": {
          "description": "A JSONPath subset locating the value to check.",
          "title": "JSONPath",
          "type": "string"
        },
        "regex": {
          "description": "A regular expression the match's text must contain.",
          "title": "Pattern",
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "description": "Fails the step unless a JSONPath match exists, equals a value or matches a regular expression.",
    "hotPathClass": "declarative",
    "kind": "assert.jsonpath",
    "label": "Assert JSON value",
    "phases": [
      "step.after"
    ],
    "version": 1
  },
  {
    "category": "assert",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "field": {
          "description": "Which part of the response to check: body, headers, url or status.",
          "enum": [
            "body",
            "headers",
            "url",
            "status"
          ],
          "title": "Field",
          "type": "string"
        },
        "mode": {
          "description": "How to compare: contains, equals or matches (regular expression).",
          "enum": [
            "contains",
            "equals",
            "matches"
          ],
          "title": "Mode",
          "type": "string"
        },
        "negate": {
          "description": "Flips the assertion's pass/fail verdict.",
          "title": "Negate",
          "type": "boolean"
        },
        "text": {
          "description": "The text to look for, per the selected mode.",
          "title": "Text",
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "description": "Fails the step unless a field contains, equals or matches text.",
    "hotPathClass": "declarative",
    "kind": "assert.contains",
    "label": "Assert text",
    "phases": [
      "step.after"
    ],
    "version": 1
  },
  {
    "category": "assert",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "maxMs": {
          "description": "Fails the step when the response takes longer than this.",
          "minimum": 0,
          "title": "Maximum response time",
          "type": "number",
          "x-vayu-unit": "ms"
        }
      },
      "required": [
        "maxMs"
      ],
      "type": "object"
    },
    "description": "Fails the step if the response took longer than a bound.",
    "hotPathClass": "declarative",
    "kind": "assert.duration",
    "label": "Assert response time",
    "phases": [
      "step.after"
    ],
    "version": 1
  },
  {
    "category": "assert",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "bytes": {
          "description": "The body size to compare against, in bytes.",
          "minimum": 0,
          "title": "Body size",
          "type": "integer",
          "x-vayu-unit": "B"
        },
        "op": {
          "description": "How the body size must compare to the bound: less than, less than or equal, greater than, greater than or equal, or equal.",
          "enum": [
            "lt",
            "lte",
            "gt",
            "gte",
            "eq"
          ],
          "title": "Comparison",
          "type": "string"
        }
      },
      "required": [
        "bytes"
      ],
      "type": "object"
    },
    "description": "Fails the step unless the response body size compares as configured.",
    "hotPathClass": "declarative",
    "kind": "assert.size",
    "label": "Assert body size",
    "phases": [
      "step.after"
    ],
    "version": 1
  },
  {
    "category": "timer",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "gaussian": {
          "additionalProperties": false,
          "description": "A randomly drawn wait following a normal distribution.",
          "properties": {
            "deviationMs": {
              "description": "How far a drawn wait typically varies from the mean, in milliseconds.",
              "minimum": 0,
              "title": "Standard deviation",
              "type": "number",
              "x-vayu-unit": "ms"
            },
            "meanMs": {
              "description": "The average wait drawn from the distribution, in milliseconds.",
              "minimum": 0,
              "title": "Mean",
              "type": "number",
              "x-vayu-unit": "ms"
            }
          },
          "required": [
            "meanMs",
            "deviationMs"
          ],
          "title": "Gaussian wait",
          "type": "object"
        },
        "maxMs": {
          "description": "The upper bound of a uniformly random wait, in milliseconds.",
          "minimum": 0,
          "title": "Maximum wait",
          "type": "integer",
          "x-vayu-unit": "ms"
        },
        "minMs": {
          "description": "The lower bound of a uniformly random wait, in milliseconds.",
          "minimum": 0,
          "title": "Minimum wait",
          "type": "integer",
          "x-vayu-unit": "ms"
        },
        "ms": {
          "description": "A fixed wait, in milliseconds, before the next step.",
          "minimum": 0,
          "title": "Wait",
          "type": "integer",
          "x-vayu-unit": "ms"
        }
      },
      "type": "object"
    },
    "description": "Waits a fixed, uniformly random or gaussian-random span before the next step, outside this step's own latency.",
    "hotPathClass": "declarative",
    "kind": "timer.think",
    "label": "Think time",
    "phases": [
      "step.between"
    ],
    "version": 1
  },
  {
    "category": "timer",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "everyMs": {
          "description": "How often this node starts, start to start, in milliseconds.",
          "minimum": 1,
          "title": "Cadence",
          "type": "integer",
          "x-vayu-unit": "ms"
        },
        "perUser": {
          "description": "Paces each virtual user independently instead of sharing one cadence across all of them.",
          "title": "Per user",
          "type": "boolean",
          "x-vayu-group": "advanced"
        }
      },
      "required": [
        "everyMs"
      ],
      "type": "object"
    },
    "description": "Holds this request, folder or collection to a steady start-to-start cadence across iterations, whatever its own duration was.",
    "hotPathClass": "declarative",
    "kind": "timer.pacing",
    "label": "Pacing",
    "phases": [
      "step.before"
    ],
    "version": 1
  },
  {
    "category": "timer",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "perUser": {
          "description": "Applies the target rate to each virtual user independently instead of sharing one budget across all of them.",
          "title": "Per user",
          "type": "boolean",
          "x-vayu-group": "advanced"
        },
        "targetPerMinute": {
          "description": "The number of times this node should start per minute.",
          "exclusiveMinimum": 0,
          "title": "Target rate",
          "type": "number"
        }
      },
      "required": [
        "targetPerMinute"
      ],
      "type": "object"
    },
    "description": "Holds this request, folder or collection to a target rate - N per minute - shared across every virtual user unless perUser is set.",
    "hotPathClass": "declarative",
    "kind": "timer.throughput",
    "label": "Throughput",
    "phases": [
      "step.before"
    ],
    "version": 1
  },
  {
    "category": "controller",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "condition": {
          "description": "A {{variable}} comparison (==, !=, matches or exists) that must hold for this step, or folder, to run.",
          "pattern": "^\\s*\\{\\{[^}]+\\}\\}\\s*(==|!=|matches)\\s*\\S.*$|^\\s*\\{\\{[^}]+\\}\\}\\s*exists\\s*$",
          "title": "Condition",
          "type": "string"
        }
      },
      "required": [
        "condition"
      ],
      "type": "object"
    },
    "description": "Skips this step, or every step of a folder it sits on, when a condition against a resolved variable is false.",
    "hotPathClass": "declarative",
    "kind": "control.if",
    "label": "If",
    "phases": [
      "step.before"
    ],
    "version": 1
  },
  {
    "category": "controller",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "type": "object"
    },
    "description": "Runs on this user's first iteration only.",
    "hotPathClass": "declarative",
    "kind": "control.once",
    "label": "Once only",
    "phases": [
      "step.before"
    ],
    "version": 1
  },
  {
    "category": "controller",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "cases": {
          "description": "A map from a variable's value to the folder member's name to route to.",
          "title": "Cases",
          "type": "object"
        },
        "default": {
          "description": "The folder member to route to when no case matches the variable's value.",
          "title": "Default case",
          "type": "string"
        },
        "variable": {
          "description": "The variable whose resolved value picks which case to route to.",
          "minLength": 1,
          "title": "Variable",
          "type": "string"
        }
      },
      "required": [
        "variable",
        "cases"
      ],
      "type": "object"
    },
    "description": "Routes to a named folder member by a variable's resolved value.",
    "hotPathClass": "declarative",
    "kind": "control.switch",
    "label": "Switch",
    "phases": [
      "step.before"
    ],
    "version": 1
  },
  {
    "category": "controller",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "everyN": {
          "description": "Runs only every Nth occurrence.",
          "minimum": 1,
          "title": "Every Nth",
          "type": "integer"
        },
        "perUser": {
          "description": "Counts occurrences per virtual user instead of sharing one counter across all of them.",
          "title": "Per user",
          "type": "boolean",
          "x-vayu-group": "advanced"
        },
        "percent": {
          "description": "Runs this share of occurrences, decided on the producer's own counter.",
          "maximum": 100,
          "minimum": 0,
          "title": "Percent",
          "type": "number",
          "x-vayu-unit": "%"
        }
      },
      "type": "object"
    },
    "description": "Runs only a share of this occurrence's calls, by percentage or every Nth one, decided on the producer's own counter.",
    "hotPathClass": "declarative",
    "kind": "control.throughput",
    "label": "Throughput",
    "phases": [
      "step.before"
    ],
    "version": 1
  },
  {
    "category": "controller",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "count": {
          "description": "How many times to walk this folder's members per iteration.",
          "minimum": 1,
          "title": "Repeat count",
          "type": "integer"
        }
      },
      "required": [
        "count"
      ],
      "type": "object"
    },
    "description": "Walks a folder's members a fixed number of times per iteration.",
    "hotPathClass": "declarative",
    "kind": "control.loop",
    "label": "Loop",
    "phases": [
      "step.between"
    ],
    "version": 1
  },
  {
    "category": "transaction",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "includeTimers": {
          "description": "Folds a between-member timer wait into the transaction's summed time.",
          "title": "Include timers",
          "type": "boolean"
        },
        "name": {
          "description": "The name this folder's summed latency is reported under.",
          "minLength": 1,
          "title": "Transaction name",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "description": "Sums a folder's own member latencies into one named transaction, reported with its own percentiles.",
    "hotPathClass": "declarative",
    "kind": "control.transaction",
    "label": "Transaction",
    "phases": [
      "step.after"
    ],
    "version": 1
  },
  {
    "category": "metric",
    "collectionOnly": false,
    "configSchema": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "description": "The name this metric is reported under, and used in a custom.<name>.<stat> threshold.",
          "maxLength": 100,
          "minLength": 1,
          "title": "Metric name",
          "type": "string"
        },
        "source": {
          "additionalProperties": false,
          "description": "Where the metric's value comes from.",
          "properties": {
            "condition": {
              "additionalProperties": false,
              "description": "Records 1 when the condition holds, 0 otherwise (for a rate metric).",
              "properties": {
                "field": {
                  "description": "Which part of the response the condition reads: status, header, jsonpath, latency or size.",
                  "enum": [
                    "status",
                    "header",
                    "jsonpath",
                    "latency",
                    "size"
                  ],
                  "title": "Field",
                  "type": "string"
                },
                "header": {
                  "description": "The response header to read, when field is header.",
                  "title": "Header name",
                  "type": "string"
                },
                "jsonpath": {
                  "description": "The JSONPath to read, when field is jsonpath.",
                  "title": "JSONPath",
                  "type": "string"
                },
                "operator": {
                  "description": "How the field's value compares against value.",
                  "enum": [
                    "eq",
                    "ne",
                    "lt",
                    "lte",
                    "gt",
                    "gte",
                    "contains"
                  ],
                  "title": "Operator",
                  "type": "string"
                },
                "value": {
                  "description": "The value the field is compared against.",
                  "title": "Value"
                }
              },
              "required": [
                "field",
                "operator",
                "value"
              ],
              "title": "Condition",
              "type": "object"
            },
            "header": {
              "description": "Reads the metric's value from this response header.",
              "title": "Header name",
              "type": "string"
            },
            "jsonpath": {
              "description": "Reads the metric's value from the response body at this JSONPath.",
              "title": "JSONPath",
              "type": "string"
            },
            "latency": {
              "description": "Records the response's latency.",
              "title": "Latency",
              "type": "boolean"
            },
            "size": {
              "description": "Records the response body's size, in bytes.",
              "title": "Body size",
              "type": "boolean"
            },
            "status": {
              "description": "Records the response's status code.",
              "title": "Status code",
              "type": "boolean"
            }
          },
          "title": "Source",
          "type": "object"
        },
        "type": {
          "description": "Whether the metric is a trend, a counter or a rate.",
          "enum": [
            "trend",
            "counter",
            "rate"
          ],
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "name",
        "type",
        "source"
      ],
      "type": "object"
    },
    "description": "Records a named trend, counter or rate from the response, reported beside the built-in phases and usable as a custom.<name>.<stat> threshold.",
    "hotPathClass": "declarative",
    "kind": "metric.record",
    "label": "Record a custom metric",
    "phases": [
      "step.after"
    ],
    "version": 1
  }
]